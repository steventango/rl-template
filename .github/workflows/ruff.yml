name: Ruff

on:
  push:
    branches: [ main ]
    paths: [ "**.py" ]
  pull_request:
    branches: [ main ]
    paths: [ "**.py" ]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
